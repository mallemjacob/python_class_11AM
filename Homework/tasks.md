# magic8Ball

## Write a function called `getAnswer()` that takes a `answerNumber` as an argument and returns the corresponding answer from a predefined list of answers.

## Use random module, generate a random number between 0 and 7, and pass that number to the `getAnswer()` function to get a random answer.

## Print the output to the console.

```python

```
